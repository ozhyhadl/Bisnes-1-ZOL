export type Testimonial = {
  name: string;
  role: string;
  quote: string;
  result: string;
};

// TODO: replace with verified customer testimonials before publishing.
export const testimonials: Testimonial[] = [];
